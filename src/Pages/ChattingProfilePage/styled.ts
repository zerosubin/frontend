import styled from "styled-components"

const Container = styled.section`
  height: 100vh;
  margin: 80px 2px;
`
const Title = styled.h3`
  font-weight: 700;
  font-size: 26px;
  margin: 18px;

  font-family: var(--font-nanumfontEB);
`

export const SC = { Container, Title }